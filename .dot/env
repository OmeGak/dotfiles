#!/usr/bin/env bash
# Sets up the minimum environment for dot steps

export DOT=$HOME/.dotfiles
export PATH="$DOT/.dot/bin:$DOT/bin:$PATH"

if [[ "$(uname -s)" == "Linux" ]]; then
  export PATH="$HOME/.linuxbrew/bin:$PATH"
fi
