#!/usr/bin/env bash
# Applies default preferences for OSX and some apps.

set -e

# Init environment
source $HOME/.dotfiles/.dot/util/env

if [[ "$(uname -s)" != "Darwin" ]]; then
  pprint fail "This is not a Mac"
  exit 1
fi

pprint intro "Applying system defaults"

$DOT/osx/defaults.sh
exit 0
