#!/usr/bin/env bash
# Checks if a descriptor is linked to target

src=$1
target=$2

# Check if source is a link
[[ ! -L "${src}" ]] && exit

# Check if is linked to target
if [[ $(readlink "${src}") == "${target}" ]]; then
  echo 'linked'
fi
