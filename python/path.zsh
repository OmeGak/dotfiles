# add `python`, `python-config`, `pip` etc. pointing to `python3`, `python3-config`, `pip3` etc.
PATH="${BREWDIR}/opt/python/libexec/bin:${PATH}"

# add pyenv shims
PATH="${HOME}/.pyenv/shims:${PATH}"
